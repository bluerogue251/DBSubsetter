package trw.dbsubsetter

object DbSubsetter extends App {
  //  val connectionString = "jdbc:postgresql://localhost:5432/db_subsetter"
  //  val dbConnection = DriverManager.getConnection(connectionString)
}
